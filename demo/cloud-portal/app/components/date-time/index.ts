import { DateTime } from './date-time';
import type { DateTimeProps } from './types';

// Export main component and types
export { DateTime };
export type { DateTimeProps };

// Export default format constant (for backward compatibility)
export { DEFAULT_DATE_FORMAT as DATE_FORMAT } from './formatters';
